README

00:00:00 Speaker: In this lecture we're going to talk about observability in the cloud and distributed architectures. Why it matters. And different architectural elements that you can bring to bear to make your application, your distributed application more resilient. So there's three key pillars that we talk about when we talk about observability metrics that I'm gathering logs and distributed tracing on the metrics. These are different metrics whether they're about the hardware or the application. But these are specific metrics that I'm going to gather CPU utilization, air rates, requested latency, load on the network. There's a whole bunch of different metrics that you can keep track of. The goal here is to define which ones are most important to you. And then logging this means that I'm gathering those metrics uh, longitudinally, which means timestamped over a period of time. There's both structured logs and unstructured events that can happen. Um, some of the logs you'll be creating yourself, and some of them will be being created by the system, like, oh, I'm out of I'm out of disk space. Um, is one that shows up in the system logs. So it's a combination of all those capturing those metrics in some kind of business or technical context. They could come from the application, or they could come from the system itself. And then distributed tracing. Distributed tracing is really important and somewhat difficult if you don't plan for it up front. This is where I'm actually tracing the user journey or specific application. Maybe it's a back office flow. I'm tracing the flow of that, um, workflow through all of its steps and through all the microservices that are going on. So having ways to gather logs across distributed systems and still see that end to end request flow is very important. And that's called distributed tracing. All right. Let's go over some other core concepts. There's Slis SLOs and SLAs. So let's talk about the difference between them. An SLA is the service level indicator. This is the measurable metric that you want to, you know um capture that's that's what it is. For example ninety nine percent latency. Right. You can capture that or CPU utilization or IO input output throughput IO throughput. Right. Um, so those are service level indicators. Service level objective is what is the target for the SLI. So if I want ninety nine percent of my requests that come in, uh, take less than two hundred milliseconds, that would be an SLO. So SLO is the objective. SLI is the metric the indicator. And then then you have a contractual guarantee which is your SLA service level agreement. This agreement that you make with us, whoever's using your service, your application or your microservice, this is hey, my SLA is I guarantee, um, ninety nine percent of my requests will happen in under two hundred milliseconds. Your SL and my total throughput, as long as my total throughput does not go above one hundred, um, gigabytes per second, that's outrageously high. That's okay. We can handle one hundred gigabytes because we have a Terabit network card. Right? So the SLA is the combination of all those objectives and what your targets are for those. Right. So SLI slo SLA right now let's talk about telemetry signals. There's different kinds of telemetry. Now telemetry is metrics that I'm gathering over time. And they they can be a lot of different metrics. They could be metrics about, um, L3 cache hit rates in the CPU. Way down deep in the CPU. CPU utilization IO utilization, um, throughput on an application a number of requests. There are so many different types of telemetry signals, it's important to understand where the telemetry signals are coming from. Are they coming from the server, the hardware itself, or are they coming from a client that's trying to access something? Because that can give you a good idea of the number of requests that your whole system can handle. So you've got to understand the difference between those client side where you're you're the client of a microservice. How often can I get a request back? And I'm getting the result back right from the client side basis. The server side, the server may be sending requests back in under, you know, two hundred milliseconds. But I may have the client side may be bogged up in maybe an API gateway, or maybe if it's direct, maybe slow network connectivity. What really matters is that you understand the difference between which metrics you're measuring and where you're getting your telemetry from, whether it's from the client side or from the server side. There is, um, a standard out there called Opentelemetry, and it's very vendor neutral, and it's an SDA SDK that can connect to all these different, um, types of telemetry producers, whether it's hardware, devices or applications. So, um, it's best to use these opentelemetry standards that are out there. Now, the good news is, is that there's lots of cloud native tools that adhere to the Opentelemetry standard, right? And they all do different types of things. Now this is where you have to be somewhat careful. Um, so like for example if I'm just generating and gathering metrics and I decide to use AWS CloudWatch and I am having AWS CloudWatch, watch all my AWS services to see that they're doing all the right things and all my metrics. And if AWS goes down, I no longer have visibility into what's going on because AWS CloudWatch is down. So a lot of times when we put in these clay cloud native tools, we use another cloud to do it, or we use an on prem service or our own service running in the cloud, possibly. So for metrics. Prometheus and AWS CloudWatch are good tools. They're for dashboards. Um, the CloudWatch insights is is available and Grafana. These are just examples. There's lots of lots of them out there. Distributed tracing. Like I said before, this one's a really important one to put into your microservice architectures up front so that you can see how something flows through the whole system. Um, Jaeger, Zipkin, AWS, X-ray are some examples of those logging. Um, there are some great logging tools out there. Elasticsearch is kind of the forefront in Kibana cloud logging. There are some new tools out there. Uh, one called Extreme Search, which you just dump all your logs into this big old hairy file system, and they've got a neuromorphic processor on the front. So you can now do forensics on all of your logs, petabytes of logs you can handle with, with some of these, uh, tools. Logging is great for forensics. When something has happened, maybe you got hacked. Maybe you've got a persistent problem that keeps occurring. But it's hard to identify exactly where. That's where you start looking at your logs and then alerting. Now, alerts are very important because you want to know as soon as something happens in your system what to do and alerting. Um, is totally event driven. Um, and there are tools out there like alert manager and Pagerduty, um, which I think these are funny names because when you if you were a sysadmin in the nineties, in the early two thousand, you had a pager. Even if you had a cell phone, you still had a pager, because we had ways of hooking those pagers up to system logs, um, when they would alert us when things would happen, either with a build that was going on or our services were down. Okay. Architectural patterns. So we talked about all the tools. Now let's talk about the architectural patterns on how I can leverage these tools, put these tools into, um, into my architecture and the best ways to do that. So the first one most common that we see is a sidecar or agent, um, a container, uh, design pattern, Um, the sidecar design patterns. Uh, we see a lot with, um, service meshes. They're kind of built in. Uh fluentd. Envoy. Opentelemetry all have these. Uh, same with Istio. Has these and other. Other service. Full blown service meshes have the sidecar design pattern. Um, it's important to understand that these do take resources. So if you are using Kubernetes and you're forming a pod, you'll want to put your sidecar in that same pod as your microservice and make sure that you have it very lightweight, that it's only doing the necessary things that it needs to because you don't want your sidecar collecting metrics, taking up all the CPU or IO. So you've got to be careful on on what you're doing. And then there's daemonset or node agents. These are when I've got a node agent that's running on every single node in my cluster, so that I can look at host level metrics and syslog so I can pump those back in. And that's that's important too. Then there's two different types of philosophy on how to get the information to a central location. There's the push and the pull. Push means agents send their telemetry to a collector. So all these node agents or sidecar agents, they're sending all their data up to an aggregator or collector, right. It's going to collect all that information. The other technique is a pool where the collector is actually going and scraping information from those nodes. So it's it's asking them, hey, give me something. Hey, give me something. What what I've seen, um, successful is a combination of kind of the both where I'm pulling periodically to make sure that that is still running, because maybe I haven't heard from it in a while, and I. Hey, what's going on? Well, nothing's happening on that box or something is down and I can't get access, so I see some combinations of the push pull. But if you're worried also about network and, um, you know, swamping your network with a bunch of telemetry, then you got to come up with some strategies on push pull or aggregation at the edge before you start sending information. Okay. Best practices. Instrument early break in the metrics and tracing from day one. This makes your life a whole lot easier. Use structured logging JSON and key value pairs are easy to parse instead of just dumping everything into a big string. Use JSON um, because it gives better context for when you're actually, um, trying to do forensics on logging or you're trying to actually build a dashboard out of it. Define your slis and your flows upfront. Focus on things that are meaningful for the user centric targets. So if you're really concerned, if you write a banking app, for example, and you're concerned about the processing time to process a payment and get a shipment out, then come up with Slis and SLOs for that. Right. Maybe you don't care about CPU utilization because all your applications really don't use a lot of CPU. Maybe they're I o bound, so you don't need to measure everything. Determine what needs to be measured. Right. And and come up with what your thresholds are on those. Right. Make sure that you have correlation of IDs, especially if you're doing a distributed tracing. You need to make sure that if you've got a workflow that's started, that you can identify that across all of your microservices and use tagging and labeling, um, that will enrich your telemetry with the services, the region, the environment that things are running in. All right. Scaling, observability. This one can be really hard. Um, if if I've got ten thousand other microservices running and I only have one collector of telemetry, I'm going to overwhelm that collector. So I know this sounds kind of weird. You need a architecture for your observability, so you need to be able to scale it. You need to determine how often you're doing, sampling how long you're keeping the logs. Um, you know, if it's highly critical, let's say, um, it's a critical system where people's lives could be in danger. Your retention policy is going to be very different than, um, dealing with, um, monitoring the weather on your house. Right? If you do, if you do that. Right. So retention policies, um, need to, need to be established as well, because you're going to generate petabytes of telemetry coming out of some of these big systems. So you got to take that into consideration. Also, if you're running in a multitenant architecture you need to take into consideration isolation of the workloads and also all the telemetry as well. You don't want to mix telemetry. You could run into some issues there. Okay. Alerting. Let's talk about alerting real quick. Make sure that you have some alerting rules. They typically have thresholds both lower limit and higher limit thresholds before you start throwing an alert. So examples I can have error rates. It's okay to have errors. But if I have errors a certain percentage of errors over five minutes then that's a high threshold. A low threshold may be, you know, a certain threshold, a certain number of errors over a ten minute period may be a lower threshold. So play with the different colors on alerts a red, a yellow and orange a green alert. Everything's hunky dory. Everything's going well, right? Document any steps of common issues that you find, and make sure that you can handle those appropriately. And then also make sure that you can automate when you hit these alerts. If I hit an alert, I need to maybe add more microservices, right? So you want to tie this back into your Kubernetes or Docker Swarm, um, tools that uh, or even your, um, your orchestrator for, uh, your cloud services to fire up new instances, whatever the case may be. And then don't forget about postmortems. You need to make sure that you're doing postmortems so that you can do continuous improvement. Maybe you've got one microservice that's causing you consistent problems. So let's take a look at an e-commerce checkout. Um, site. Right. First seeing what do I do first. Grab metrics. What are my metrics that I'm going to check my SLA is the checkout success rate. Let's just say that's it. Checkout success rate. What kind of metric is that? Client side Inside metric. Write client side telemetry. Write ninety fifth percentile payment latency under two seconds. That's my slow right now. My tracing. What am I going to do on tracing? I'm going to trace the map, the customer journey from the cart to the payment gateway to inventory, and maybe even to a ship notification back to the user via email or on the website. Okay. And then I look at the logging. What am I going to log? I'm going to capture all the payment provider responses and error codes. Okay. Then what dashboards do I want to create from all this stuff? Hey I'm going to use a Grafana dashboard. I'm going to look at real time KPIs to see how all of my e-commerce site is is working appropriately. And if how many errors I'm getting where it's above the metrics that I've established. Okay. So that's how it works. That's observability. You've got to build this into your architecture up front. If you try and bolt it on afterwards, you end up rewriting a lot of code or worse, you bolt it on the side and it becomes this monstrosity that's pulling a lot of information, and it creates more complexity than it really needs to be. Make sure that you're leveraging metrics and logs that may already exist, and tracing doing distributed tracing through the whole flow. Also, make sure that you're defining those Slis and SLOs and automating any alerts that you need. Continuously look at ways to make things easier and better through looking at your dashboards, and also try and reduce any noise that you're getting in your dashboards. Dashboards. There's nothing worse than a whole bunch of false negatives. And then it leads to false positives, which are even worse. So all right, good luck with your observability architecture.